# Copy to .env.production — used by npm run build / make github
# Create a separate token with your GitHub Pages URL in URL restrictions, e.g.
#   https://<username>.github.io/<repo-name>/*
PUBLIC_MAPBOX_TOKEN=
